--- Singular/configure.in.orig	2014-07-20 23:20:03.321074004 +1200
+++ Singular/configure.in	2014-07-20 23:19:10.610962222 +1200
@@ -69,7 +69,7 @@
 		FLINT_CFLAGS="-I${FLINT_HOME}/include/flint"
 		FLINT_LIBS="-L${FLINT_HOME}/lib -lflint -lmpfr -lmpir"
 	else
-		FLINT_CFLAGS=
+		FLINT_CFLAGS="-I${FLINT_HOME}/include/flint"
 		FLINT_LIBS="-lflint"		
 	fi	
 	CFLAGS="${BACKUP_CFLAGS} ${FLINT_CFLAGS}" 
@@ -1406,6 +1406,7 @@
 if test "x$flint_found" = "xyes"; then
   includedir= "${FLINT_HOME}/include ${includedir}"
   LDFLAGS="${LDFLAGS} ${FLINT_LIBS}"
+  SLDFLAGS="${SLDFLAGS} ${FLINT_LIBS}"
   NEED_LIBS="-lflint -lmpfr ${NEED_LIBS}"
 fi
 
--- factory/flint-check.m4.orig	2014-07-22 10:04:16.354652516 +1200
+++ factory/flint-check.m4	2014-07-22 10:05:53.234832327 +1200
@@ -53,7 +53,7 @@
 		FLINT_CFLAGS="-I${FLINT_HOME}/include/flint"
 		FLINT_LIBS="-L${FLINT_HOME}/lib -lflint -lmpfr -lmpir"
 	else
-		FLINT_CFLAGS=
+		FLINT_CFLAGS="-I${FLINT_HOME}/include/flint"
 		FLINT_LIBS="-lflint"		
 	fi	
 	CFLAGS="${BACKUP_CFLAGS} ${FLINT_CFLAGS}" 
@@ -115,8 +115,6 @@
 
 AC_LANG_POP
 
-AM_CONDITIONAL(SING_HAVE_FLINT, test "x$HAVE_FLINT" = "xyes")
-
 CFLAGS=${BACKUP_CFLAGS}
 LIBS=${BACKUP_LIBS}
 #unset LD_LIBRARY_PATH
--- factory/configure.in.orig	2014-07-22 10:04:04.814631057 +1200
+++ factory/configure.in	2014-07-22 10:05:29.314787988 +1200
@@ -16,6 +16,7 @@
 AC_CONFIG_HEADER(config.h)
 AC_CONFIG_AUX_DIR(bin)
 AC_CONFIG_MACRO_DIR([.])
+m4_include([flint-check.m4])
 #
 # Do not change version here!!!
 #
